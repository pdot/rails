%h1 Editing Membership

- form_for(@membership) do |form|
  = render :partial => 'form', :locals => {:form => form}
  %p= form.submit 'Update'

%p
  = link_to 'Show', @membership
  = link_to 'Back', memberships_path
